#include<bits/stdc++.h>
using namespace std;

void solution() {

    int  n; cin>>n;
    int sum = pow(2, n-1);

    cout << sum << endl;
     
}

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    solution();
     
    return 0;
}